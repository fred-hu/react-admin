let defaultState = {};
let {{ name }} = (state = defaultState, action) => {

    switch (action.type) {
        case '':
            return Object.assign({},state,{});
        default:
            return state;
        }
    };
export default {{ name }};